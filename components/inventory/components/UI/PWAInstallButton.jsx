// PWA géré au niveau du PORTAIL (dashboard), pas dans l'inventaire — bouton neutralisé.
import React from 'react';

export function PWAInstallButton() {
  return null;
}

export default PWAInstallButton;
